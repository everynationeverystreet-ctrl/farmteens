# TODO

- [x] Redesign header navigation into one unified floating glass dock (desktop) and ensure active “Home” styling matches spec.

- [ ] Update `index.html` markup to support left/center/right dock layout while keeping existing JS hooks (`#menuToggle`, `#siteNav`, `.nav-link`).
- [ ] Update `styles.css` to add premium glassmorphism dock styles and remove desktop pill backgrounds for nav links.
- [ ] Preserve mobile hamburger behavior inside the dock container and keep CTA aligned.
- [ ] Quick manual test: check layout at ~1200px, ~860px, and ~620px breakpoints; verify no overlap and consistent spacing.

